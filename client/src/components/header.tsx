import React from "react";
import Logout from "../pages/logout";

const header = () => {
  return (
    <header>
      <div>Header</div>
      <Logout />
    </header>
  );
};

export default header;
